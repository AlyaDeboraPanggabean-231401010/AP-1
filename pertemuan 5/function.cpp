#include <iostream>
#include "function_header.h" // header berisi data, operasi, atau fungsi yang kita buat sendiri
#include "prepocessor.h" // penulisannya #include "namafile"

// using namespace std;
u; // contohnya di sini, pada file "prepocessor.h", kita buat using namespace std jadi "u"

// function dgn nilai kembalian
// tipe_data nama_fungsi(parameter)
// ..
// }

int jumlah (int a, int b) {
    // cout << a + b << endl;
    return a + b;
}

int kurang (int a, int b) {
    // cout << a - b << endl;
    return a - b ;
}

// int bagi (int a, int b) {
//     // cout << a - b << endl;
//     return a / b ;
// }

void bintang() {
    cout << "*********" << endl;
}

//prosedur .. fungsi yg gk punya nilai kembalian
void garis() { 
    cout << "===========" << endl;
}

begin
    s;
    int n ;
    o l jumlah (2, 3) l e;
    o l kurang (2, 3) l e;
    o l kali (2, 3) l e; // kali dan bagi ada di file "function_header.h"
    o l bagi (2, 3) l e;
    o l PI l e;
    o l "Masukkan angka : ";
    i r n ;
    bintang ();
    o l n l e;
    // garis ();
    // bintang ();
end